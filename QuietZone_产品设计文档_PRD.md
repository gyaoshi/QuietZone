# QuietZone — 主动降噪 App 产品设计文档 (PRD)

> **产品代号**: QuietZone
> **版本**: v1.0
> **文档日期**: 2026-05-24
> **文档状态**: Draft

---

## 1. 产品概述

### 1.1 产品定义

QuietZone 是一款运行于 Android 手机上的**空间主动降噪应用**，利用手机麦克风采集环境噪声，通过高性能 FxLMS 自适应滤波算法实时生成反噪声信号，经手机扬声器或外接音箱播出，在用户周围形成一个低噪声的"静区"。

### 1.2 产品定位

| 维度 | 描述 |
|------|------|
| **品类** | 工具类音频应用 |
| **目标用户** | 受环境低频噪声困扰的人群（办公室、工厂、交通、居家） |
| **核心价值** | 无需专用硬件，一部手机即可实现 10-25dB 低频降噪 |
| **差异化** | 开放空间降噪（非耳机式）、支持外接音箱扩展、Hybrid ANC 混合模式 |
| **技术门槛** | C++ FxLMS + NEON SIMD + Oboe 低延迟音频 |

### 1.3 产品愿景

> **让每个人都能用手机创造自己的安静空间。**

### 1.4 非目标

- ❌ 不做耳机式降噪（已有 AirPods / Sony WH 等成熟方案）
- ❌ 不做录音/播放器功能
- ❌ 不做社交/分享功能
- ❌ 不支持蓝牙音箱（延迟 >100ms，不满足 ANC 因果性约束）

---

## 2. 用户研究

### 2.1 目标用户画像

| 画像 | 场景 | 痛点 | 期望 |
|------|------|------|------|
| **办公室白领** | 开放式办公室空调/通风噪声 | 无法集中注意力 | 工位附近降低低频背景噪声 |
| **工厂/车间工人** | 机械设备持续运转噪声 | 听力损伤风险、疲劳 | 降低工作位噪声 10-15dB |
| **居家用户** | 窗外交通/施工低频噪声 | 影响睡眠、休息 | 卧室窗边降噪 |
| **图书馆/自习学生** | 空调嗡嗡声、人员走动 | 学习效率下降 | 桌面局部静区 |
| **音频/视频创作者** | 录音环境低频噪声干扰 | 录音质量差 | 录音位降噪提升录音品质 |

### 2.2 用户旅程

```
发现 → 安装 → 首次使用 → 日常使用 → 进阶使用
 │       │        │          │          │
 │       │        │          │          └─ 外接音箱 / 参数微调
 │       │        │          └─ 一键开关 / 模式切换
 │       │        └─ 授权麦克风 → 校准 → 降噪生效
 │       └─ 权限授予（麦克风必需）
 └─ 应用商店搜索"主动降噪"
```

### 2.3 核心用户场景

#### 场景 A：办公室工位降噪

```
角色：张先生，程序员
环境：开放式办公室，头顶空调出风口持续低频噪声
流程：
  1. 手机放在工位桌上，麦克风朝向空调方向
  2. 打开 QuietZone，选择"Hybrid混合"模式
  3. 点击启动按钮，等待约3秒校准
  4. 手机扬声器发出反噪声波，工位区域噪声明显降低
  5. 查看实时降噪量：15.2 dB
  6. 下班后一键关闭
```

#### 场景 B：居家窗边降噪 + 外接音箱

```
角色：李女士，居家办公
环境：临街卧室，窗外持续交通低频噪声
流程：
  1. 手机放在窗台上，3.5mm 接口连接桌面有源音箱
  2. 打开 QuietZone，开启"外接音箱模式"
  3. 输出增益自动调至 2.0x，启动校准
  4. 音箱播放反噪声，卧室窗边区域噪声降低约 20dB
  5. 睡前调低增益至 1.5x，降低反噪声自身可听度
```

---

## 3. 功能规格

### 3.1 功能架构图

```
QuietZone
├── 🔊 核心降噪
│   ├── 一键启停
│   ├── ANC 模式选择（前馈 / 反馈 / 混合）
│   ├── 次级路径自动校准
│   └── 实时降噪量监测
│
├── 📊 实时监测
│   ├── 降噪量仪表 (dB)
│   ├── 处理延迟显示 (μs)
│   ├── 收敛状态指示
│   └── 参考vs残差频谱图
│
├── ⚙️ 参数调节
│   ├── 步长 μ 滑块
│   ├── 输出增益滑块
│   └── 外接音箱开关
│
├── 🔧 设备管理
│   ├── 音频输出路由（扬声器 / 有线 / USB-C）
│   ├── 音频输入源选择
│   └── 外接音箱校准向导
│
└── 📱 系统服务
    ├── 前台保活服务
    ├── 通知栏控制
    └── 权限管理
```

### 3.2 功能优先级 (MoSCoW)

| 优先级 | 功能 | 版本 |
|--------|------|------|
| **Must** | 一键启停 ANC | v1.0 |
| **Must** | Hybrid 混合降噪模式 | v1.0 |
| **Must** | 麦克风权限申请 | v1.0 |
| **Must** | 实时降噪量显示 | v1.0 |
| **Must** | 次级路径自动校准 | v1.0 |
| **Should** | 前馈/反馈模式切换 | v1.0 |
| **Should** | 外接音箱模式 | v1.0 |
| **Should** | 实时频谱显示 | v1.0 |
| **Should** | 步长/增益参数调节 | v1.0 |
| **Should** | 前台服务 + 通知栏控制 | v1.0 |
| **Could** | 输出设备路由选择 | v1.1 |
| **Could** | 降噪效果录音对比 | v1.1 |
| **Could** | 预设场景（办公室/卧室/工厂） | v1.1 |
| **Could** | 降噪定时器 | v1.2 |
| **Won't** | 蓝牙音箱支持 | — |
| **Won't** | 云端同步 | — |

### 3.3 核心功能详细规格

---

#### F-01: 一键启停 ANC

| 属性 | 说明 |
|------|------|
| **触发** | 点击主界面中央圆形按钮 |
| **前置条件** | 已授权 RECORD_AUDIO 权限 |
| **流程** | 点击 → 启动 ANCService → 麦克风录音开始 → FxLMS 处理 → 反噪声输出 → 状态变为"运行中" |
| **反馈** | 按钮从▶️变为⏹，颜色从青色变为红色，状态文字"运行中" |
| **停止** | 再次点击 → 停止 ANCService → 静音输出 → 状态变为"待机" |
| **异常** | 无麦克风权限 → 弹出权限请求弹窗 |

---

#### F-02: ANC 模式选择

| 模式 | 图标 | 推荐场景 | 降噪带宽 | 预期降噪量 |
|------|------|---------|---------|-----------|
| **前馈** | 🎤→🔊 | 噪声方向已知 | 宽带 20-2000Hz | 10-15 dB |
| **反馈** | 🔊🔄 | 周期/窄带噪声 | 窄带 50-500Hz | 8-12 dB |
| **混合 ★** | 🎤+🔊 | 全场景（推荐） | 宽+窄 20-2000Hz | 15-25 dB |

- 默认选中"混合"模式
- 运行中可实时切换，切换时自动 reset 滤波器权重并重新收敛
- 切换过程约 2-5 秒重新收敛

---

#### F-03: 次级路径校准

| 属性 | 说明 |
|------|------|
| **自动校准** | 首次启动或切换输出设备时自动触发 |
| **校准流程** | 播放 0.5s 白噪声脉冲 → 录音 → LMS 辨识 Ŝ(z) → 完成 |
| **校准时长** | 约 2-3 秒 |
| **用户提示** | "正在校准，请保持安静..." |
| **手动校准** | 设置页提供"重新校准"按钮 |
| **外接音箱** | 连接新设备时必须重新校准 |

---

#### F-04: 实时监测面板

| 指标 | 单位 | 刷新率 | 说明 |
|------|------|--------|------|
| **降噪量** | dB | 10Hz | $10 \log_{10}(P_{ref}/P_{err})$，正值为降噪 |
| **处理延迟** | μs | 10Hz | 单帧 FxLMS 处理耗时 |
| **收敛状态** | — | 2Hz | 未收敛(橙) / 已收敛(绿)，阈值 3dB |
| **频谱图** | dB | 4Hz | 128 点 FFT，参考(蓝) vs 残差(绿) |

降噪量颜色编码：

| 降噪量 | 颜色 | 含义 |
|--------|------|------|
| > 10 dB | 🟢 绿色 | 优秀 |
| 3-10 dB | 🟠 橙色 | 有效 |
| < 3 dB | 🔴 红色 | 无效/收敛中 |

---

#### F-05: 外接音箱模式

| 属性 | 说明 |
|------|------|
| **触发** | 参数面板"外接音箱"开关 |
| **自动检测** | v1.1 预留，检测有线/USB-C 音频设备插入事件 |
| **效果** | 输出增益自动调至 2.0x（可手动调至 4.0x）|
| **校准** | 自动触发重新校准次级路径 |
| **延迟要求** | ≤ 5ms（仅支持有线/USB-C，不支持蓝牙） |
| **推荐设备** | 3.5mm 有源音箱、USB-C 桌面音箱、有源低音炮 |

---

### 3.4 输入输出规格

#### 音频输入

| 参数 | 值 | 说明 |
|------|-----|------|
| 采样率 | 48000 Hz | 标准音频采样率 |
| 位深 | 16-bit / Float | PCM_FLOAT 优先 |
| 声道 | 单声道 | Mono |
| 源 | VOICE_RECOGNITION | 最低延迟、无预处理 |
| 缓冲区 | 128 frames | ≈2.67ms 延迟 |

#### 音频输出

| 参数 | 值 | 说明 |
|------|-----|------|
| 采样率 | 48000 Hz | 与输入同步 |
| 位深 | Float | PCM_FLOAT |
| 声道 | 单声道 | Mono |
| 模式 | MODE_STREAM | 流式输出 |
| 缓冲区 | 128 frames | 与输入同步 |

---

## 4. 交互设计

### 4.1 信息架构

```
主界面 (Single-Page)
│
├── Header
│   ├── 品牌标题 "主动降噪"
│   └── 状态指示灯 (🟢运行中 / ⚪待机)
│
├── 主控区
│   └── 大圆形启停按钮 (120dp)
│
├── 统计面板
│   ├── 降噪量 (dB)
│   ├── 处理延迟 (μs)
│   ├── 收敛状态
│   └── 降噪进度条
│
├── 模式选择
│   ├── [前馈] [反馈] [混合★]
│   └── 选中高亮
│
├── 频谱面板
│   ├── 实时双曲线频谱
│   └── 频率轴标签
│
├── 参数面板
│   ├── 步长 μ 滑块
│   ├── 输出增益滑块
│   └── 外接音箱开关
│
└── 算法信息
    └── 只读技术参数表
```

### 4.2 页面布局 (暗色主题)

```
┌──────────────────────────────┐
│  主动降噪              🟢 运行中 │  ← Header
├──────────────────────────────┤
│                              │
│         ┌─────────┐          │
│         │    ▶️    │          │  ← 主控按钮 (120dp圆形)
│         │  开始降噪 │          │
│         └─────────┘          │
│      点击开始降噪             │
├──────────────────────────────┤
│  实时统计                     │
│  ┌──────┬──────┬──────┐      │
│  │15.2dB│ 245μs│ 已收敛 │      │  ← 三指标
│  │降噪量 │ 延迟  │ 状态  │      │
│  └──────┴──────┴──────┘      │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░ 50%     │  ← 降噪进度条
├──────────────────────────────┤
│  降噪模式                     │
│  ┌──────┬──────┬──────┐      │
│  │ 前馈  │ 反馈  │混合 ★ │      │  ← 模式Chips
│  └──────┴──────┴──────┘      │
├──────────────────────────────┤
│  实时频谱              🔵参考 🟢残差 │
│  ┌──────────────────────┐    │
│  │〰〰〰〰〰〰〰〰〰〰〰〰 │    │  ← 频谱图 (Canvas)
│  │ 〰〰〰〰〰〰〰〰〰〰  │    │
│  └──────────────────────┘    │
│  0Hz    6kHz   12kHz  24kHz  │
├──────────────────────────────┤
│  参数调节                     │
│  步长 μ: 0.0100               │
│  ●─────────────○ 0.001~0.1   │  ← 滑块
│  输出增益: 1.0x               │
│  ●──────○───────── 0.5~4.0   │
│                               │
│  外接音箱模式          [关]   │  ← 开关
│  增大输出功率，适合外接有源音箱 │
├──────────────────────────────┤
│  算法原理                     │
│  核心算法  Leaky FxLMS        │
│  滤波器     FIR 256阶         │
│  加速       ARM NEON SIMD     │
│  音频引擎   Oboe (AAudio)     │
│  ...                          │
└──────────────────────────────┘
```

### 4.3 交互状态矩阵

| 状态 | 主控按钮 | 模式选择 | 参数调节 | 频谱 |
|------|---------|---------|---------|------|
| **未授权** | 灰色，点击弹权限请求 | 禁用 | 禁用 | 空白 |
| **待机** | 青色▶️，可点击 | 可选择 | 可调节 | 空白 |
| **校准中** | 橙色⏳，不可点击 | 禁用 | 禁用 | 校准动画 |
| **运行-收敛中** | 红色⏹，可点击停止 | 可切换 | 可调节 | 实时更新 |
| **运行-已收敛** | 红色⏹，可点击停止 | 可切换 | 可调节 | 实时更新 |
| **错误** | 红色⚠️，点击重试 | 禁用 | 禁用 | 错误提示 |

### 4.4 关键交互流程

#### 启动流程

```
点击▶️
  │
  ├─ 无麦克风权限？
  │   └─ 弹出系统权限弹窗
  │       ├─ 授权 → 继续
  │       └─ 拒绝 → 提示"降噪需要麦克风权限"→ 按钮恢复
  │
  ├─ 启动 ANCService (前台服务)
  │
  ├─ 次级路径校准 (2-3秒)
  │   └─ 提示"正在校准..."
  │
  ├─ 校准完成 → ANC 引擎启动
  │
  └─ 状态: 运行中 (收敛中 → 已收敛)
```

#### 外接音箱连接流程

```
开启"外接音箱"开关
  │
  ├─ 检测音频输出路由
  │   ├─ 有线/USB-C → 继续
  │   └─ 蓝牙 → 弹窗警告"蓝牙延迟过大，降噪效果受限"
  │
  ├─ 输出增益自动调至 2.0x
  │
  ├─ 触发次级路径重新校准
  │
  └─ 提示"请将音箱放置在噪声源与您之间"
```

---

## 5. 视觉设计规范

### 5.1 设计语言

- **风格**: 暗色科技感，沉浸式仪表盘
- **灵感**: 航空仪表 / 音频工作站 / 科幻 HUD

### 5.2 色彩系统

| 用途 | 色值 | 说明 |
|------|------|------|
| **背景** | `#0A0E17` | 深蓝黑 |
| **卡片** | `#141B2D` | 暗蓝灰 |
| **卡片浅** | `#1A2340` | 悬停/选中 |
| **主强调** | `#00E5FF` | 青色，品牌色 |
| **成功** | `#00E676` | 绿色，已收敛/降噪优秀 |
| **警告** | `#FF9100` | 橙色，收敛中/降噪一般 |
| **危险** | `#FF1744` | 红色，停止/降噪无效 |
| **文字主** | `#FFFFFF` | 白色 |
| **文字辅** | `#B0BEC5` | 灰色 |
| **参考频谱** | `#42A5F5` | 蓝色 |
| **残差频谱** | `#66BB6A` | 绿色 |
| **降噪曲线** | `#FFCA28` | 黄色 |

### 5.3 字体

| 用途 | 字号 | 字重 |
|------|------|------|
| 页面标题 | 28sp | Bold |
| 卡片标题 | 18sp | Bold |
| 指标数值 | 18sp | Bold |
| 正文 | 14sp | Regular |
| 辅助文字 | 12sp | Regular |
| 频率轴标签 | 10sp | Regular |

### 5.4 间距与圆角

| 元素 | 圆角 | 内边距 |
|------|------|--------|
| 主控按钮 | 50% (圆形) | — |
| 卡片 | 16dp | 16dp |
| 模式 Chip | 12dp | 12dp |
| 频谱 Canvas | 8dp | — |
| 进度条 | 4dp | — |

---

## 6. 技术架构

### 6.1 分层架构

```
┌─────────────────────────────────────────────┐
│                   UI 层                      │
│   Jetpack Compose (ANCScreen.kt)            │
│   · 状态管理 (remember / mutableStateOf)     │
│   · 实时数据刷新 (LaunchedEffect)            │
├─────────────────────────────────────────────┤
│                 服务层                        │
│   ANCService.kt (前台服务)                    │
│   · 生命周期管理                              │
│   · 通知栏控制                               │
├─────────────────────────────────────────────┤
│                音频管理层                      │
│   AudioStreamManager.kt                      │
│   · AudioRecord / AudioTrack 管理             │
│   · 音频线程优先级                            │
│   · 设备路由                                 │
├─────────────────────────────────────────────┤
│                JNI 桥接层                     │
│   ANCEngine.kt ↔ anc_engine.cpp              │
│   · Kotlin ↔ C++ 数据传递                     │
├─────────────────────────────────────────────┤
│               C++ 算法层                      │
│   ┌─────────────────────────────────┐        │
│   │ AudioProcessor                  │        │
│   │  ├── FxLMSFilter (前馈)         │        │
│   │  ├── FxLMSFilter (反馈)         │        │
│   │  ├── SecondaryPathEstimator     │        │
│   │  └── SpectrumAnalyzer           │        │
│   └─────────────────────────────────┘        │
│   优化: ARM NEON SIMD / -O3 / -ffast-math    │
├─────────────────────────────────────────────┤
│               音频驱动层                      │
│   Oboe → AAudio (8.0+) / OpenSL ES (旧版)    │
│   · MMAP 低延迟模式                          │
│   · Exclusive 独占模式                       │
└─────────────────────────────────────────────┘
```

### 6.2 数据流

```
麦克风 ──→ AudioRecord ──→ float[] micInput
                                │
                    ┌───────────┴───────────┐
                    │   C++ FxLMS Engine     │
                    │                        │
                    │  1. x(n) → W_ff → y_ff │
                    │  2. e(n) → W_fb → y_fb │
                    │  3. y = y_ff + y_fb    │
                    │  4. Ŝ * x → x̂(n)      │
                    │  5. w += μ·x̂·e        │
                    │  6. FFT → spectrum     │
                    └───────────┬───────────┘
                                │
              float[] speakerOutput ──→ AudioTrack ──→ 扬声器/音箱
                                │
                    ┌───────────┴───────────┐
                    │   Stats + Spectrum     │
                    │   → UI 实时更新        │
                    └───────────────────────┘
```

### 6.3 线程模型

| 线程 | 优先级 | 职责 |
|------|--------|------|
| **UI 线程** | Normal | Compose 渲染、用户交互 |
| **音频线程** | THREAD_PRIORITY_URGENT_AUDIO | 麦克风采集 → FxLMS → 输出 |
| **统计线程** | Background | 每 100ms 更新一次统计/频谱 |

### 6.4 性能指标

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| 端到端延迟 | < 5ms | 麦克风→扬声器环路 |
| FxLMS 单帧处理 | < 500μs | ANCStats.processingTimeUs |
| CPU 占用 | < 15% (单核) | Android Profiler |
| 内存占用 | < 50MB | Android Profiler |
| 电池消耗 | < 5%/小时 | 48kHz 持续运行 |
| 收敛时间 | < 3秒 | 恒定噪声源 |
| 降噪量 (Hybrid) | 15-25 dB @ 50-500Hz | 自噪声测试 |

---

## 7. 权限与安全

### 7.1 权限清单

| 权限 | 级别 | 必要性 | 说明 |
|------|------|--------|------|
| `RECORD_AUDIO` | 危险 | **必须** | 采集环境噪声，ANC 核心功能依赖 |
| `MODIFY_AUDIO_SETTINGS` | 普通 | 推荐 | 控制音频路由 |
| `FOREGROUND_SERVICE` | 普通 | **必须** | 后台持续运行 ANC |
| `FOREGROUND_SERVICE_MEDIA_PLAYBACK` | 普通 | **必须** | Android 14+ 前台服务类型 |
| `POST_NOTIFICATIONS` | 危险 | 推荐 | Android 13+ 通知栏控制 |
| `BLUETOOTH_CONNECT` | 危险 | 可选 | LE Audio 外接音箱 |

### 7.2 隐私说明

- **麦克风数据仅用于本地实时处理**，不录音、不存储、不上传
- 反噪声信号为实时计算生成，不包含任何个人信息
- 频谱数据仅用于本地 UI 显示

---

## 8. 版本规划

### 8.1 v1.0 — MVP (当前)

**核心功能**：
- ✅ Hybrid ANC 一键启停
- ✅ 三模式切换（前馈/反馈/混合）
- ✅ 实时降噪量监测
- ✅ 实时频谱显示
- ✅ 参数调节（步长/增益）
- ✅ 外接音箱模式
- ✅ 前台服务保活

### 8.2 v1.1 — 场景优化

- 📋 输出设备自动检测与路由
- 📋 预设场景（办公室/卧室/工厂/交通）
- 📋 降噪前后录音对比功能
- 📋 次级路径校准可视化
- 📋 Widget 桌面小部件（一键开关）

### 8.3 v1.2 — 智能化

- 📋 噪声类型自动识别 → 自动选择最优模式
- 📋 自适应步长（根据噪声特性动态调整 μ）
- 📋 定时器（睡前降噪定时关闭）
- 📋 历史降噪效果统计

### 8.4 v2.0 — 深度学习增强

- 📋 Deep ANC 模型集成（TFLite 推理）
- 📋 非线性失真补偿
- 📋 多麦克风波束成形参考
- 📋 空间降噪区域可视化

---

## 9. 测试方案

### 9.1 算法验证测试

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| 纯音降噪 | 100/200/500Hz 正弦波，测量 e(n) 功率 | > 15dB 降噪 |
| 窄带降噪 | 风扇噪声录制回放 | > 10dB 降噪 |
| 宽带降噪 | 白噪声 + 滤波模拟 | > 5dB 降噪 |
| 收敛速度 | 阶跃噪声，计时到达 3dB | < 3秒 |
| 稳定性 | 持续运行 1 小时 | 无崩溃、无发散 |

### 9.2 性能测试

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| 延迟 | 环路测试 (扬声器→麦克风) | < 5ms |
| CPU | Android Profiler 持续运行 | < 15% |
| 电池 | 100% → 持续运行计时 | > 20小时 |
| 采样连续 | 检查 buffer underrun | 0 次/分钟 |

### 9.3 兼容性测试

| 维度 | 范围 |
|------|------|
| Android 版本 | 8.0 (API 26) — 15 (API 35) |
| 芯片平台 | Snapdragon / Dimensity / Exynos / Kirin |
| 音频 API | AAudio (8.0+) / OpenSL ES (降级) |
| 外接设备 | 3.5mm 音箱 / USB-C 音频 / USB DAC |

---

## 10. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 手机扬声器低频输出不足 | 高 | 高 | 外接音箱模式；UI 提示建议外接 |
| 延迟超标导致算法不收敛 | 中 | 高 | Oboe 独占模式；减少 blockSize；提示用户关闭其他音频应用 |
| 不同手机音频延迟差异大 | 高 | 中 | 启动时自动测延迟；延迟过大弹窗警告 |
| 扬声器→麦克风声学反馈 | 中 | 中 | 次级路径估计；Leaky 防发散；反馈消除 |
| 电池消耗过快 | 中 | 中 | 降低采样率选项；自适应帧率 |
| 用户对降噪效果期望过高 | 高 | 中 | 说明书明确有效频段(50-2000Hz)；首次使用引导 |

---

## 11. 附录

### A. 术语表

| 术语 | 全称 | 说明 |
|------|------|------|
| ANC | Active Noise Cancellation | 主动降噪 |
| FxLMS | Filtered-x Least Mean Square | 滤波-x 最小均方算法 |
| FIR | Finite Impulse Response | 有限脉冲响应滤波器 |
| 次级路径 | Secondary Path | 扬声器→误差麦克风的传递函数 |
| 主路径 | Primary Path | 参考麦克风→误差麦克风的传递函数 |
| 收敛 | Convergence | 自适应滤波器权重趋于最优 |
| Leaky | Leaky Factor | 泄漏因子，防止权重漂移 |
| NEON | ARM NEON | ARM SIMD 指令集 |
| Oboe | — | Google 低延迟音频 C++ 库 |
| AAudio | — | Android 原生低延迟音频 API |

### B. 竞品分析

| 竞品 | 类型 | 降噪方式 | 空间降噪 | 算法 | 价格 |
|------|------|---------|---------|------|------|
| **AirPods Pro** | 耳机 | 耳内封闭+ANC | ❌ | 私有 | ¥1,899 |
| **Sony WH-1000XM5** | 耳机 | 耳内封闭+ANC | ❌ | 私有 | ¥2,499 |
| **Bose QC Ultra** | 耳机 | 耳内封闭+ANC | ❌ | 私有 | ¥2,999 |
| **Silentium** | 空间降噪 | 外置扬声器+ANC | ✅ | 商业方案 | B2B |
| **QuietZone (本项目)** | 手机App | 手机/外接音箱 | ✅ | FxLMS 开源 | 免费 |

**差异化核心**: 无需专用硬件，手机即工具，空间降噪（非耳机式）

### C. 参考文献

详见 [ANC_算法研究与技术实现文档.md](./ANC_算法研究与技术实现文档.md) 第9节

---

*文档结束 — QuietZone v1.0 PRD*
